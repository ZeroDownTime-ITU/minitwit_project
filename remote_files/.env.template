# In order to use this - Create a .env file and do "cp .env.template .env" (don't forget to cd into remote_files first)
# this will make it so that we all have the same env. Now you just have to type in the values manually 
# maybe there is a better way, but yeah, so far, so good
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
 
JDBC_URL=jdbc:postgresql://postgres-db:5432/minitwit
VERSION=latest