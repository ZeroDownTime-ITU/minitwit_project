{
  "name": "MiniTwit Dev",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "ms-azuretools.vscode-docker"
      ]
    }
  },
  "postCreateCommand": "git config --global --add safe.directory /workspace && mkdir -p data && mvn install -DskipTests"
}