server {
    listen 80;
    server_name minitwit.app www.minitwit.app;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name minitwit.app www.minitwit.app;

    ssl_certificate /etc/letsencrypt/live/minitwit.app/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/minitwit.app/privkey.pem;

    location / {
        proxy_pass http://svelte-frontend:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api {
        proxy_pass http://java-backend:7070;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /web {
        proxy_pass http://java-backend:7070;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}